# The GPU lane: the offline lane plus the CUDA build of PyTorch, pinned by the unit that adds the GPU path and
# recorded with the driver/runtime matrix in docs/guides.
-r requirements-precompute.txt

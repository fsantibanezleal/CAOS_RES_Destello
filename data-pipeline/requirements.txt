# Offline pipeline engines, pinned to the verified install (Python 3.12). Added by the unit that uses each one.
numpy==2.4.6
pyarrow==25.0.0
pandas==3.0.5
# the simulation engine, a separate package (CAOS_FlyCNS), pinned by its release tag until it is on PyPI
flycns @ git+https://github.com/fsantibanezleal/CAOS_FlyCNS@v0.06.000

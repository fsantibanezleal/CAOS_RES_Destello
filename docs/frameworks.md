# Frameworks

One card per engine or library the product actually calls, added in the same change that starts calling it: what it
is and why it was chosen, its version and licence, how it is installed and configured here, and a runnable example.

| Framework | Role | Card |
|---|---|---|
| flycns | connectome compiler, eye model and whole-CNS simulation (Python and browser) | added with the unit that first calls it |

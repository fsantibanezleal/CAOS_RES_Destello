# frontend

The companion web: six pages on the shared app shell, the App workbench, the focus route per case, and the in-app
architecture panel. It is built in its own units of the plan (the web base, the 3D brain, the eyes and the fly, the
method tabs and the live lane), each with its feature design in `docs/design/features/`.
